---
id: w-619e
status: closed
deps: []
links: []
created: 2026-01-06T20:20:29Z
type: task
priority: 2
---
# (Duplicate) Add Copilot CLI in sandbox image

Duplicate of `ccs-9f4a`.

Change: keep Codex at `@latest` by default; add GitHub Copilot CLI via npm (`@github/copilot@prerelease`).

Test plan:
- make image
- podman run --rm <image> codex --version
- podman run --rm <image> copilot --help

## Acceptance Criteria

- Image installs `@github/copilot@prerelease` globally and exposes its CLI on PATH.
- Build succeeds in typical environments.
